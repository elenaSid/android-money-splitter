<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data class="AddFamilyActivityBinding"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/alabaster_background"
            app:navigationIcon="@drawable/ic_back"
            app:titleTextColor="@color/tundora_text"/>

        <LinearLayout
            android:id="@+id/llInput"
            android:layout_width="match_parent"
            android:layout_height="90dp"
            android:layout_below="@+id/toolbar"
            android:layout_marginTop="-10dp"
            android:background="@drawable/shadow_bottom"
            android:orientation="vertical">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <EditText
                    android:id="@+id/edtBabyName"
                    style="@style/SplitterAppTheme.TextView.Normal_32"
                    android:layout_width="match_parent"
                    android:layout_height="90dp"
                    android:layout_gravity="center_vertical|start"
                    android:layout_marginStart="16dp"
                    android:layout_marginEnd="16dp"
                    android:background="@android:color/transparent"
                    android:hint="@string/family_add_hint"
                    android:inputType="textPersonName"
                    android:textColorHint="@color/black17_text"
                    android:theme="@style/SplitterAppTheme.BlueAccent"/>
            </LinearLayout>
        </LinearLayout>
    </RelativeLayout>
</layout>